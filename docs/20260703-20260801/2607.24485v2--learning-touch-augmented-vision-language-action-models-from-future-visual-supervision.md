---
title: "τ: Learning Touch-Augmented Vision-Language-Action Models from Future Visual Supervision"
title_zh: τ：从未来视觉监督中学习触觉增强的视觉-语言-动作模型
authors: "Ning Cheng, Jinan Xu, Wanlin Li, Yangzhi Chen, Jing Gao, Yiqun Wang, Kelan Peng, Wenjuan Han"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24485v2"
tags: ["query:vvvpg"]
score: 9.0
evidence: 触觉增强的视觉-语言-动作模型，用于接触丰富的机器人操作
tldr: 触觉对接触丰富的操作任务至关重要，但现有VLA模型难以有效利用高维触觉信号。为此提出τ框架，受JEPA启发，从未来视觉监督中学习动作条件下的时空触觉表示，并在训练中融合视觉语言特征生成动作。实验表明τ在四类任务上优于现有模型，并泛化到未知物体和场景，提升操作性能与鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24485v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1810, \"height\": 1004, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24485v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1839, \"height\": 355, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24485v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1820, \"height\": 1219, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24485v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 877, \"height\": 581, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24485v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 832, \"height\": 476, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24485v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1822, \"height\": 438, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24485v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1810, \"height\": 296, \"label\": \"Table\"}]"
motivation: 现有方法多处理瞬时接触或低维力/力矩序列，高维触觉信号与预训练VLA模型的适配不足，且任务数据有限。
method: 提出τ框架，利用JEPA式未来视觉监督学习动作条件触觉表示，并与视觉语言特征融合生成动作，训练后无额外推理开销。
result: 在引入的TacAura数据集四类操作任务上，τ优于现有模型，且对未见物体和场景有良好泛化。
conclusion: 未来视觉监督能有效引导触觉表示学习，提升接触丰富型操作中VLA模型的性能与泛化能力。
---

## 摘要
将触觉感知融入视觉-语言-动作（VLA）模型，对于接触丰富的操作任务具有前景，在这些任务中，仅依靠视觉观察往往无法捕捉到关于物理互动的关键线索。然而，在有限的特定任务数据下，学习信息丰富的触觉表征并将其有效适配到预训练的VLA模型仍然具有挑战性。现有方法要么聚焦于瞬时接触状态，要么使用6D力/力矩序列建模时间交互动态，导致高维触觉信号未被充分探索。为了解决这些挑战，我们提出了τ，一个触觉增强的VLA框架，受联合嵌入预测架构（JEPA）启发，从未来视觉监督中学习动作条件下的时空触觉表征，并将其与视觉-语言特征融合以生成动作。这种监督在潜在空间中运行，且仅在训练期间使用，不增加部署开销。我们还引入了TacAura，一个包含视觉、本体感觉和基于视觉的触觉信号的数据集，涵盖四个代表性的接触丰富操作任务。实验表明，τ优于现有模型，并能泛化到未见过的物体和场景，提供了改进的操作性能和鲁棒性。

## Abstract
Incorporating tactile sensing into Vision-Language-Action (VLA) models holds promise for contact-rich manipulation, where visual observations alone often fail to capture critical cues about physical interactions. However, learning informative tactile representation while effectively adapting it to pretrained VLA models remains challenging under limited task-specific data. Existing methods either focus on instantaneous contact states or model temporal interaction dynamics using 6D wrench sequences, leaving high-dimensional tactile signals underexplored. To address these challenges, we present τ, a touch-augmented VLA framework that learns an action-conditioned spatiotemporal tactile representation from future visual supervision inspired by the Joint-Embedding Predictive Architecture (JEPA), and fuses it with vision-language features for action generation. This supervision operates in latent space and is used only during training, adding no deployment overhead. We also introduce TacAura, a dataset of synchronized vision, proprioception, and vision-based tactile signals across four representative contact-rich manipulation tasks. Experiments show that τ outperforms existing models and generalizes to unseen objects and scenes, delivering improved manipulation performance and robustness.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究动机**：接触丰富的机器人操作（如插拔、按压、擦拭）中，仅依赖视觉信息难以捕捉接触状态、力分布和形变等关键物理交互线索；触觉传感能提供这些信息，但如何高效学习触觉表征并融入预训练的视觉-语言-动作（VLA）模型，仍是一个未解决难题。
- **现有方法局限**：
  - 多数方法仅利用瞬时接触状态或低维 6D 力/力矩序列，忽视了高维视觉触觉信号（如 GelSight 类传感器输出）中的细粒度空间接触模式；
  - 在有限的任务特定数据下，直接适配预训练 VLA 模型容易出现表征不兼容、过拟合等问题；
  - 部分世界模型方法显式预测未来触觉信号，计算开销大。
- **研究意义**：提出一种在训练阶段利用未来视觉特征变化作为监督信号、学习动作条件下的动态触觉表征，并将其与视觉-语言特征融合用于动作生成的新框架，在不增加推理开销的前提下提升接触丰富操作的性能与泛化能力。

## 2. 方法论：τ 框架

- **核心思想**：受 JEPA（联合嵌入预测架构）启发，通过“动作条件 + 未来视觉特征变化预测”的辅助自监督任务，迫使触觉表征编码交互动态，而非静态接触状态；该辅助分支仅在训练时使用，推理时移除。
- **模型架构**（三个关键组件）：
  1. **预训练 VLA 骨干**：基于 π0.5，接收多视角 RGB 图像、语言指令和本体感觉状态，条件流匹配生成未来动作块；
  2. **触觉编码与适配模块**：使用共享的触觉编码器（由 π0.5 视觉编码器初始化）处理左右手指的视觉触觉信号（法向 + 切向图），输出触觉 token，再经线性适配器投影到 VLA 的语义空间；
  3. **JEPA 式预测自监督分支**（训练专用）：将触觉 token 与动作序列编码后的 action token 拼接，经轻量 MLP 预测器预测未来视觉特征变化；预测目标由 VLA 视觉编码器对当前和未来图像提取特征并做差分得到。
- **关键技术细节**：
  - 使用“特征变化”而非绝对特征作为预测目标，并施加 stop-gradient 防止表示退化；
  - 根据触觉变化幅度对预测项加权，突出接触状态显著变化的时刻（权重范围 0.2–5.0）；
  - 总损失：`L_total = L_IL + λ L_SSL`，其中 `L_IL` 为条件流匹配模仿学习损失，`λ = 0.3`；
  - 推理时仅保留 VLA 骨干和触觉适配模块，无额外计算。

## 3. 实验设计

- **数据集**：自建的 **TacAura** 数据集，包含四个接触丰富任务：插头插入、USB 插入、印章按压、白板擦拭。每任务 100 条人工遥操作演示，物体位姿有变化；同步记录多视角 RGB-D（两个第三人称 + 一个腕部相机）、左右手指视觉触觉信号（DM-Tac WS，320×240，~40 FPS）、本体感觉和语言指令，统一时间戳并下采样到 10 Hz。
- **评测场景**：真实机器人（Franka Research 3 + Franka Hand + 定制 3D 打印触觉传感器适配器），每个任务 20 次试验，按任务阶段（抓取/对齐/插入，或抓取/接触/按压/擦拭）统计部分成功率和完整成功率。
- **对比方法**：
  - 视觉 VLA 基线：π0、π0.5；
  - 触觉/力感知多模态策略：ForceVLA†、ForceFlow†、T-Rex†（†表示适配其触觉传感设置但保留原框架）；
  - 自方法变体：τ-Wrist Sup.（腕部视觉监督）、τ-Front Sup.（前视监督）、τ-DualView Sup.（双视监督）。
- **泛化测试**：对 USB 插入和白板擦拭两个任务进行零样本物体泛化（两个未见物体）和场景泛化（两组未见干扰物集合）。

## 4. 资源与算力

- **论文未明确提及**具体 GPU 型号、数量或训练时长（仅说明训练 30,000 步，未给出具体卡时）。这是信息缺口。
- 论文在结论中提到“计算资源限制”导致未与触觉世界模型方法比较，暗示其算力有限，但未量化。

## 5. 实验数量与充分性

- **实验组数**：
  - 主要对比实验：5 个基线 + 3 个自变体，在 4 个任务上共 20 个试验/模型/任务，覆盖三阶段成功率；
  - 泛化实验：2 个任务 ×（2 个未见物体 + 2 个未见干扰物设置），共 8 种测试条件；
  - 消融实验：3 个消融（去掉动作序列条件、去掉 SSL、去掉触觉模块），在 4 个任务上执行。
- **充分性评价**：
  - 覆盖了任务阶段分解和整体成功率，对比公平（基线尽量适配同一触觉设置）；
  - 消融设计合理，验证了各组件贡献；
  - 但每个任务仅 20 次试验，统计波动可能较大；未报告方差或多次运行的平均/标准差，结果可靠性有提升空间；
  - 只评估了单一机器人平台和一种触觉传感器，跨传感器/跨本体泛化未验证。

## 6. 主要结论与发现

- τ 显著优于所有基线：最佳变体 τ-Wrist Sup. 平均完整成功率 71.25%，最高基线 T-Rex† 为 31.25%，绝对提升 40 个百分点。
- 基线模型主要瓶颈在最终阶段（插入、按压、擦拭完成），而 τ 保留了从中间阶段到最终阶段的高成功率，表明触觉动态表征对精细接触推理有效。
- 监督视图存在任务依赖：前视监督更适合插入类任务（目标定位），腕视监督更适合按压/擦拭类任务（接触感知）。
- 泛化实验显示：白板擦拭对物体和场景变化鲁棒（几乎无下降），而 USB 插入对未见物体和场景干扰敏感，仍有下降。
- 消融表明：触觉模块贡献最大（移除后平均成功率降 42.5 个百分点）；SSL 和动作序列条件也分别贡献 20 和 12.5 个百分点。

## 7. 优点

- **方法设计新颖**：将 JEPA 式预测自监督引入触觉-VLA 学习，以未来视觉特征变化为监督，避免了像素级重建开销，且训练专用零推理成本。
- **高维触觉信号处理**：针对视觉触觉传感器的高分辨率空间模式建模，超越了仅用 6D wrench 的方法。
- **数据基础设施贡献**：开源遥操作工具、同步转换工具和 TacAura 数据集，有助于社区复现和后续研究。
- **实验设计完整**：包括任务阶段分解、多基线对比、消融、零样本泛化，并能解释不同视图监督的作用。
- **实现有效**：在真实机器人上取得大幅性能提升，且具备一定泛化能力。

## 8. 不足与局限

- **算力资源未披露**：没有给出 GPU 型号、数量、训练时间，影响可复现性和效率评估。
- **实验统计力度有限**：每任务仅 20 次试验，无方差/置信区间，结果可能受随机性影响。
- **泛化测试任务偏少**：仅测试两个任务的泛化，且插入类任务对未见物体/场景鲁棒性仍较弱。
- **跨任务、跨本体、跨传感器迁移未探索**：模型是否适用于其他机器人或触觉传感器未知；论文将其列为未来工作。
- **与触觉世界模型方法比较缺失**：受计算资源限制，未与 Dream-Tac 等世界模型对比，使其相对优势的结论不够完整。
- **双视监督融合效果不佳**：τ-DualView 反而比单视变体差，说明简单拼接双视特征在有限数据下可能引入噪声，融合机制有待改进。
- **任务难度受限**：数据集为单臂固定场景下的四类基础操作，未涉及双臂协作或更复杂操作。

（完）
